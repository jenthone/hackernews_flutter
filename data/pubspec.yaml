name: data
description: Data layer for hackernews Flutter project.

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  dio:
  json_annotation:
  retrofit:

  get_it:
  injectable:
  freezed:

dev_dependencies:
  effective_dart:
  build_runner:
  json_serializable:
  retrofit_generator:
  injectable_generator:
