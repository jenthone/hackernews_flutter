name: data
description: Data layer for hackernews Flutter project.

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  dio:
  json_annotation:
  retrofit:

  get_it: ^5.0.1
  injectable: ^1.0.5

dev_dependencies:
  build_runner:
  json_serializable:
  retrofit_generator:
  injectable_generator:
