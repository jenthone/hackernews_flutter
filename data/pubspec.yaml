name: data
description: Data layer for hackernews Flutter project.

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  domain:
    path: ../domain

  dio:
  json_annotation:
  retrofit:

dev_dependencies:
  build_runner:
  json_serializable:
  retrofit_generator:
